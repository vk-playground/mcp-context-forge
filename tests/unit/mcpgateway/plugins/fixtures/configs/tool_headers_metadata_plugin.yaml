plugins:
  # Self-contained Search Replace Plugin
  - name: "HeadersMetaDataPlugin"
    kind: "tests.unit.mcpgateway.plugins.fixtures.plugins.headers.HeadersMetaDataPlugin"
    description: "A tools header plugin."
    version: "0.1"
    author: "MCP Context Forge Team"
    hooks: ["tool_pre_invoke", "tool_post_invoke"]
    tags: ["plugin", "headers"]
    mode: "enforce"  # enforce | permissive | disabled
    priority: 150
    conditions:
      # Apply to specific tools/servers
      - prompts: []
        server_ids: []  # Apply to all servers
        tenant_ids: []  # Apply to all tenants

# Plugin directories to scan
plugin_dirs:
  - "plugins/native"      # Built-in plugins
  - "plugins/custom"      # Custom organization plugins
  - "/etc/mcpgateway/plugins"  # System-wide plugins

# Global plugin settings
plugin_settings:
  parallel_execution_within_band: true
  plugin_timeout: 30
  fail_on_plugin_error: true
  enable_plugin_api: true
  plugin_health_check_interval: 60
